#! /usr/bin/env python

import argparse

from commands import reindex

parser = argparse.ArgumentParser(
    prog='git reindex',
    version='git-reindex 0.6.1',
    description=reindex.__doc__,
    epilog='for more detail, use: git help reindex'
)

parser.parse_args()
reindex.reindex()
